# 첨부 전용 브랜치 — fix/order-basket-product-guard

주문 화면 크래시(KIO-SCHOOL-D1) 수정 PR 본문에서 참조하는 재현 영상만 담고 있습니다.
소스 코드는 없습니다.

| 파일 | 내용 |
|---|---|
| `media/before-crash.*` | 수정 전 — 상품 삭제 후 주문 화면 복귀 시 크래시 |
| `media/after-fixed.*` | 수정 후 — 정상 렌더 |
| `media/b-before-deadend.*` | 자동 복귀 조건 변경 전 — 빈 장바구니 화면에 갇힘 |
| `media/b-after-autoreturn.*` | 변경 후 — 주문 화면으로 자동 복귀 |

`.gif` 는 PR 본문 인라인용, `.webm` 은 원본(390x844)입니다.
로컬 dev 서버 + 목 백엔드로 재현했고, 등장하는 주점·메뉴는 전부 더미 데이터입니다.

> 이 브랜치를 삭제하면 PR 본문의 영상도 같이 깨집니다.
> PR을 기록으로 남겨둘 생각이면 브랜치도 함께 두시고, 정리해도 되는 시점에 지워주세요.
