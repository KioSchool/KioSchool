import { FocalPoint } from '@@types/index';

export const WORKSPACE_IMAGE_SLOT_COUNT = 3;

export const DEFAULT_FOCAL_POINT: FocalPoint = { x: 50, y: 50 };

export const ORDER_IMAGE_HEIGHT_PX = 200;

// 손님이 주문 화면을 여는 대부분의 환경이 휴대폰이라, 편집 프레임을 모바일 폭에 맞춘다.
export const POSITION_EDITOR_FRAME_WIDTH_PX = 375;
